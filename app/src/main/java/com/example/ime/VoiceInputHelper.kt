package com.example.ime

/**
 * Helper class for voice input in UVIME.
 * Type-aliased to [VoiceInputManager] to maintain full backward compatibility
 * while exposing the upgraded silence timeouts and continuous dictation configuration.
 */
typealias VoiceInputHelper = VoiceInputManager
